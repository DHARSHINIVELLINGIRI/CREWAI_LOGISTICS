# ADD THESE LINES TO YOUR EXISTING requirements.txt
# These are the NEW packages needed for tracking module

# MCP (Model Context Protocol)
mcp==0.5.0
fastmcp==0.1.0

# Async HTTP
httpx==0.27.0

# Database
pymongo==4.6.0
motor==3.3.2

# Environment
python-dotenv==1.0.0

# Optional for scheduling (if you want scheduled monitoring)
APScheduler==3.10.4

# Logging
python-json-logger==2.0.7

# Note: Keep all your existing dependencies
# Just add the above lines to the end of your current requirements.txt
flask==2.3.2
httpx==0.24.1
streamlit==1.28.0
streamlit-option-menu==0.3.6
streamlit-lottie==0.0.5
pandas==2.0.3
pymongo==4.5.0
python-dotenv==1.0.0
crewai==0.1.0
crewai-tools==0.1.0
langchain==0.1.0
langchain-google-genai==0.0.10